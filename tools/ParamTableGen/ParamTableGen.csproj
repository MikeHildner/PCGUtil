<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Deliberately NOT in the solution: this tool is run by hand when the vendor docs
         change, and the app must build without the git-ignored kronos-manuals/ present.
         Run (from the repo root):  dotnet run [dash][dash]project tools/ParamTableGen -->
  </PropertyGroup>

</Project>
