<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Parameter tables generated by tools/ParamTableGen from the (git-ignored) vendor
         SysEx docs. Committed outputs: the build never needs the docs themselves. -->
    <EmbeddedResource Include="Resources\*.json.gz" />
  </ItemGroup>

</Project>
